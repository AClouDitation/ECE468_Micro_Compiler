PROGRAM loop1
BEGIN
    FUNCTION VOID main ()
    BEGIN
        STRING newline := "\n";
        STRING space := " ";
        INT a, b, c, d;
        READ(a,b,c,d);
        b := 29;
--        FOR (a:=0; a<b; a:=a+1)
        a:=0;
        WHILE (a<b)
            c:=b+15;
            d:=a*4;
            WRITE(a, space, b, space, c, space, d, newline);
            a:=a+1;
--        ENDFOR
        ENDWHILE
        WRITE (a,newline,b,newline,c,newline,d,newline);
    END
END
