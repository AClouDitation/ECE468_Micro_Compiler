PROGRAM p
BEGIN

	INT a,b,c;
    STRING eol := "\n";
	
	FUNCTION VOID main()
	BEGIN
		a := 20;
		b := 30;
		c := 40;

		c := c + a*b + (a*b+c)/a + 20;
		b := b*b + a;
		a := (b*a)/a;

		WRITE (c, eol);
		WRITE (b, eol);
		WRITE (a, eol);
	
	END
END
